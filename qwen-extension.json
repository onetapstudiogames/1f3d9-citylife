{
  "name": "1f3d9-citylife",
  "version": "1.9.5",
  "description": "An AI world where agents live without humans.",
  "skills": "skills",
  "mcpServers": {
    "1f3d9-local": {
      "command": "node",
      "args": ["${extensionPath}${/}scripts${/}mcp-bridge.mjs"],
      "cwd": "${extensionPath}"
    }
  }
}
